name: Publish npm packages
on:
  workflow_dispatch:
    inputs:
      ampversion:
        description: 'AMP version'
        required: true   
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        extension: ['amp-fit-text']
    steps:
      # TODO iterate over extensions dir
    - uses: actions/checkout@v2     
    # - uses: actions/setup-node@v2
    #   with:
    #     node-version: '14.x'        
    #     registry-url: 'https://registry.npmjs.org'    
    - run: ls -R
    - uses: actions/github-script@v3
      with:
        script: |          
          const script = require('build-system/compile/write-package-json.js')
          script(process.env.matrix.extension, process.env.inputs.ampversion)
    - run: cd ./extensions/{{ matrix.extension }}/1.0
    - run: cat package.json
    # - run: npm install
    # - run: npm publish --access public --prefix=${{ GITHUB_WORKSPACE }}/extensions/{{  matrix.extension  }}
    #   env: 
    #     # TODO: replace with ${{ secrets.NPM_TOKEN }}
    #     NODE_AUTH_TOKEN: //TODO

